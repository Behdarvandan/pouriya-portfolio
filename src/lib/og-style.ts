/**
 * Shared visual tokens for code-generated OG images (next/og's ImageResponse
 * runs outside the app's CSS, so these mirror globals.css's color tokens by
 * value rather than by reference).
 */
export const ogColors = {
  canvas: "#0a0a0a",
  ink: "#f5f5f5",
  muted: "#a3a3a3",
  accent: "#10b981",
};

export const ogSize = { width: 1200, height: 630 };
