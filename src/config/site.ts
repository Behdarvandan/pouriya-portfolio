/**
 * Single source of truth for the production origin. Every absolute URL
 * (metadataBase, sitemap, robots, JSON-LD) reads from here so a future
 * custom domain only requires updating this one line.
 */
export const SITE_URL = "https://pouriya-portfolio.vercel.app";
